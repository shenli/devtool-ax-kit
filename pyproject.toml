[project]
name = "devtool-ax"
version = "0.1.0"
description = "DevTool Agent-Experience Kit for testing Agent-Native developer tools"
requires-python = ">=3.11"

[project.scripts]
devtool-ax = "agent_run.cli:main"
